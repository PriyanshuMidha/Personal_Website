const TechBadge = ({ children }) => (
  <span className="inline-flex items-center rounded-full border border-border-soft bg-accent-surface px-3 py-1 text-xs font-medium text-text-secondary">
    {children}
  </span>
);

export default TechBadge;
